// This is the main FluxIndex package that includes Core, Infrastructure, and SDK.
// Users can install this single package to get all core functionality.
// For additional providers, install FluxIndex.AI.*, FluxIndex.Storage.*, etc. separately.

namespace FluxIndex
{
    /// <summary>
    /// Main FluxIndex package - includes Core, Infrastructure, and SDK components
    /// </summary>
    public static class FluxIndex
    {
        public const string Version = "0.1.0-preview";
    }
}