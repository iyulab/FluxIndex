<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <PackageId>FluxIndex.Integrations.FileFlux</PackageId>
    <IsPackable>true</IsPackable>
    <Description>FileFlux document parsing/chunking integration for FluxIndex — DI wiring and the document processing pipeline.</Description>
    <PackageTags>RAG;VectorStore;FluxIndex;FileFlux;Integration</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\FluxIndex.SDK\FluxIndex.SDK.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FileFlux" />
    <PackageReference Include="Microsoft.Extensions.Options" />
  </ItemGroup>

</Project>
