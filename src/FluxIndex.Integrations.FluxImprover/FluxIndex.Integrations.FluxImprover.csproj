<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <PackageId>FluxIndex.Integrations.FluxImprover</PackageId>
    <IsPackable>true</IsPackable>
    <Description>FluxImprover chunk quality enhancement integration for FluxIndex — DI wiring and the enrichment pipeline.</Description>
    <PackageTags>RAG;VectorStore;FluxIndex;FluxImprover;Integration</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\FluxIndex.SDK\FluxIndex.SDK.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FluxImprover" />
    <PackageReference Include="Microsoft.Extensions.Options" />
  </ItemGroup>

</Project>
