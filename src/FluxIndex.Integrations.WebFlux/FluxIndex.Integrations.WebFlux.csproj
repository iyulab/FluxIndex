<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <PackageId>FluxIndex.Integrations.WebFlux</PackageId>
    <IsPackable>true</IsPackable>
    <Description>WebFlux web-content ingestion integration for FluxIndex — DI wiring and context builder extensions.</Description>
    <PackageTags>RAG;VectorStore;FluxIndex;WebFlux;Integration</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\FluxIndex.SDK\FluxIndex.SDK.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="WebFlux" />
    <PackageReference Include="Microsoft.Extensions.Options" />
  </ItemGroup>

</Project>
